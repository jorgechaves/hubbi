## 1. Admin — tabela de painéis

- [x] 1.1 Remover importação do shadcn `Table` de `app/(admin)/admin/panels/page.tsx`
- [x] 1.2 Substituir `<Table>` pelo padrão de tabela nativa com `font-mono-brand` headers, `hover:bg-muted/50`, border entre linhas
- [x] 1.3 Adicionar empty state centralizado quando não há painéis

## 2. Admin — tabela de grupos

- [x] 2.1 Remover importação do shadcn `Table` de `app/(admin)/admin/groups/page.tsx`
- [x] 2.2 Substituir `<Table>` pelo mesmo padrão de tabela nativa (mesmas classes da de painéis)
- [x] 2.3 Adicionar empty state centralizado quando não há grupos

## 3. Dashboard — container e grid

- [x] 3.1 Trocar `max-w-2xl` por `max-w-5xl` no container do dashboard
- [x] 3.2 Adicionar `xl:grid-cols-4` ao grid de cartões de painéis
